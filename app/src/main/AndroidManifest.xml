<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a15041867.visitormanagementsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label=" Login ">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ManagerActivity"
            android:label=" Manager Activity"/>
        <activity android:name=".VisitorInfoActivity"
            android:label="Visitor Info"/>
        <activity android:name=".evacuationActivity"
            android:label="Evacuation Report"/>
        <activity android:name=".manageActivity"
            android:label="Manage Activity"/>
        <activity android:name=".summaryActivity"
            android:label="Summary Activity"/>
        <activity android:name=".RegisterActivity"
            android:label="Register"/>
        <activity android:name=".PreRegisterActivity"
            android:label="Pre Register"/>
        <activity android:name=".addActivity"
            android:label="Add User"/>
        <activity android:name=".userInfoActivity"
            android:label="User Info"/>
        <activity android:name=".CancelPreRegister"
            android:label="Cancel Pre-register"/>
        <activity
            android:name=".SignInActivity"
            android:label="Sign In"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SignOutActivity"
            android:label="Sign Out"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>